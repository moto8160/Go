# 初心者が知っておくべき Go の特徴（他言語比較つき）

## 1. コンパイル言語（実行が速い）

- Goは事前にコンパイルして実行します。
- 比較: Python/JavaScriptは主にインタプリタ型で、実行速度は一般にGoより遅め。

## 2. シンプルな言語仕様

- 機能を絞っていて覚えやすい（ジェネリクスは2022年に導入され、最小限の設計）。
- 比較: C++/Javaは機能が多く、学習コストが高い傾向。

## 3. ガベージコレクション（GC）

- メモリ管理は自動。
- 比較: C/C++は手動でメモリ管理が必要。

## 4. 並行処理が強い（goroutine と channel）

- 軽量スレッドのgoroutineを簡単に作成できる。
- channelで安全にデータをやり取りできる。
- 比較: Java/C#はスレッドやロックが中心で、記述が長くなりやすい。

## 5. 静的型付け + 型推論

- 型は明確に決まるが、`:=`で書く量を減らせる。
- 比較: Python/JavaScriptは動的型付けで実行時エラーが起きやすい。

## 6. 依存管理が標準化（Go Modules）

- `go mod`で公式に依存管理が可能。
- 比較: JavaScriptはnpm/yarn、Pythonはpip/poetryなど複数の選択肢がある。

## 7. クロスコンパイルが簡単

- 環境変数を変えるだけで他OS向けにビルド可能。
- 比較: C/C++は環境ごとの設定が複雑になりやすい。

## 8. 標準ライブラリが充実

- HTTP、暗号、テストなどが標準で揃っている。
- 比較: Python/JavaScriptは外部ライブラリ依存が多くなりがち。

## 9. フォーマットが公式で統一（gofmt）

- コーディングスタイルがほぼ自動で統一される。
- 比較: JavaScriptはESLint/Prettierなど複数の流派がある。

## 10. ビルド成果物が単一バイナリ

- 依存をまとめて1ファイルになることが多い。
- 比較: Python/Node.jsは実行時にランタイムや依存が必要。

---

### ざっくりまとめ

- **速くてシンプル**
- **並行処理が書きやすい**
- **標準ツールが揃っている**

必要なら、この内容を基に「学習ロードマップ」や「Goでよく使う構文集」も追加できます。
